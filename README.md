### Creating the Virtual Environment

```bash
python3.11 -m venv py_ml_env
```

This will create a new virtual environment in the `py_ml_env/` directory.

### Activating the Virtual Environment
- On **Linux/macOS**:
  ```bash
  source py_ml_env/bin/activate
  ```